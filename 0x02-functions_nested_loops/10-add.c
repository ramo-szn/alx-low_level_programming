#include "main.h"
#include <stdio.h>

/**
 * main - a function that adds two integers and returns the result
 * Return: Always 0.
 */
int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}
